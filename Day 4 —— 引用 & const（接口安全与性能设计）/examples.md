## 示例 1：不使用 const 的坏接口

```cpp
void printUser(User u) {
    cout << u.name << endl;
}
```

问题：

* 发生一次拷贝
* 无法表达只读意图

---

## 示例 2：改为 const 引用（推荐）

```cpp
void printUser(const User& u) {
    cout << u.name << endl;
}
```

收益：

* 零拷贝
* 语义清晰
* 更安全

---

## 示例 3：性能对比实验（必须做）

```cpp
struct Big {
    int data[1000];
};
```

```cpp
void f1(Big b);
void f2(const Big& b);
```

测试：

* 循环调用 1,000,000 次
* 对比耗时

👉 你会直观看到 const 引用的价值

---

## 示例 4：什么时候必须用指针

```cpp
void maybeProcess(User* u) {
    if (!u) return;
}
```

语义：

* u 可能不存在

👉 **指针用于表达“可空”**

---
